# FOXSCAN Site

Site vitrine + blog SEO (articles).

Contenu attendu:
- pages marketing
- landing pages
- articles référencement

Prototype importé:
- `index.html` : landing page marketing FOXSCAN
- `login.html` : page de connexion web (Sign in with Apple + redirection dashboard)
- `js/foxscan-client.js` : client API backend (JWT access/refresh + session web)
- `js/login.js` : Sign in with Apple puis échange token via `POST /auth/apple`
